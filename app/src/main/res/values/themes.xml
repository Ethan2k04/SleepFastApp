<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SnoreTracker" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/bg_grey</item>
        <item name="android:navigationBarColor">@color/bg_grey</item>
    </style>

    <style name="Theme.SnoreTracker" parent="Base.Theme.SnoreTracker" />
</resources>